<div dir="rtl">

# 📦 گزارش بازطراحی زبان‌یار

> مرداد ۱۴۰۵ · commit `ca767f5` · [zabanyar-seven.vercel.app](https://zabanyar-seven.vercel.app)
> اسناد مرتبط: [`UI-AUDIT.md`](./UI-AUDIT.md) (ممیزی) · [`DESIGN-SYSTEM.md`](./DESIGN-SYSTEM.md) (سیستم طراحی)

---

## ۱. فهرست فایل‌های تغییریافته

**۴۵ فایل · ۲ فایل جدید · ۲ سند جدید**

### فایل‌های جدید
| فایل | نقش |
|---|---|
| `src/components/ui/BridgeRing.tsx` | عنصر امضا — حلقهٔ پل |
| `DESIGN-SYSTEM.md` | سیستم طراحی |
| `UI-AUDIT.md` | ممیزی مرحله صفر |

### زیرساخت
| فایل | تغییر |
|---|---|
| `src/app/globals.css` | بازنویسی کامل توکن‌ها: پالت جدید، مقیاس تایپوگرافی/شعاع/سایه، قاعده `:focus-visible`، skip-link، انیمیشن‌های بازخورد، `prefers-reduced-motion` |
| `src/components/ui/index.tsx` | افزودن `Button`، `ButtonLink`، `ErrorState`، `SkeletonText/List/Card/Chat`؛ بازنویسی `Empty`، `Progress`، `Alert`، `LevelBadge` |
| `src/app/(app)/layout.tsx` | skip-link + `<main id="main">` + فاصله نوار پایین |
| `src/app/layout.tsx` · `public/manifest.json` | `themeColor` → `#0e7c8a` |

### به تفکیک صفحه

| صفحه | فایل‌ها | تغییر |
|---|---|---|
| **ناوبری (همه صفحات)** | `AppNav.tsx` | نوار پایین موبایل، شیت «بیشتر»، IA جدید، `aria-current`، بستن با Escape |
| **داشبورد** | `(app)/dashboard/page.tsx`, `CoachPanel.tsx` | hero «کار امروز» با منطق کار بعدی، حلقهٔ پل، آمار ثانویه؛ CoachPanel از اسلب آبی به کارت آرام + حالت خطا |
| **درس** | `LessonExercises.tsx`, `(app)/lessons/[id]/page.tsx`, `(app)/lessons/page.tsx` | بازخورد آیکون‌دار ✓/✗، حالت خطای ثبت نتیجه، حلقهٔ پل در نتیجه |
| **تعیین سطح** | `PlacementTest.tsx` | بازخورد آیکون‌دار، حلقهٔ پل برای سطح CEFR |
| **لغات** | `VocabReview.tsx` | حالت خطا با تلاش دوباره (بدون از دست رفتن امتیاز)، رنگ‌های AA |
| **مربی** | `TutorChat.tsx` | تفکیک حباب‌ها + برچسب فرستنده، نشانگر تایپ، خطای سیستمی به‌جای پیام جعلی مربی، `SkeletonChat` |
| **پیشرفت** | `(app)/progress/page.tsx`, `BadgeShelf.tsx`, `ActivityChart.tsx`, `SkillRadar.tsx` | پالت AA، `BadgeShelf` با حالت خطا و اسکلتون هم‌شکل |
| **تکالیف** | `(app)/assignments/page.tsx`, `WritingWorkshop.tsx` | حالت خالی واقعی با CTA، بج‌های AA |
| **تلفظ** | `PronunciationWorkshop.tsx`, `PronunciationPractice.tsx` | حالت خالی با CTA، رنگ نمره AA |
| **گروه** | `GroupChat.tsx`, `GroupLobby.tsx` | آواتارهای AA (۶ رنگ بررسی‌شده)، `ms-` منطقی |
| **ورود/ثبت‌نام** | `AuthForm.tsx`, `page.tsx` | پالت جدید |
| **مدرس/مدیر** | `teacher/*` (۵ فایل), `admin/page.tsx` | پالت AA، `text-start`، `me-`/`ms-` |
| **سراسری** | `BadgeToast.tsx`, `InstallPrompt.tsx`, `OfflineActions.tsx`, `OnboardingWizard.tsx`, `Speak.tsx`, `Leaderboard.tsx`, `GenerateLessonButton.tsx`, `lib/gamification.ts` | موقعیت منطقی، عدم تداخل با نوار پایین، فلش‌های اصلاح‌شده |

---

## ۲. قبل/بعد — سه صفحه کلیدی

### 🏠 داشبورد

| | قبل | بعد |
|---|---|---|
| **سلسله‌مراتب** | ۴ کارت آمار هم‌وزن + پنل مربی آبی پررنگ که بصری‌ترین عنصر صفحه بود | یک hero فیروزه‌ای «کار امروز» با حلقهٔ پل — تنها عنصر غالب |
| **راهنمایی** | کاربر نمی‌دانست از کجا شروع کند | منطق کار بعدی: هدف روزانه ← لغت سررسیده ← تکلیف ← ادامه درس، با دکمه مستقیم |
| **پنل مربی** | گرادیان آبی تمام‌عرض، رقیب بصری | کارت سفید با لبه فیروزه‌ای؛ اطلاعات همان، وزن بصری کمتر |
| **حالت خالی** | «هنوز درسی نساخته‌اید» به‌صورت یک خط متن خاکستری | کامپوننت `Empty` با آیکون، توضیح و دکمه «ساخت اولین درس» |
| **دسکتاپ** | — | آمار هفته داخل hero (فضای خالی سمت چپ پر شد) |

فایل‌ها: `shots/before-dashboard.png` ↔ `shots/after-dashboard.png`

### ✏️ تمرین (صفحه درس)

| | قبل | بعد |
|---|---|---|
| **بازخورد** | فقط رنگ حاشیه (`#10b981` / `#f43f5e`) — نقض WCAG 1.4.1 | ✓/✗ + رنگ + متن مخصوص صفحه‌خوان («پاسخ درست» / «پاسخ شما — نادرست») |
| **رنگ خطا** | `#f43f5e` قرمز تند | `#B4453C` آجری — خوانا ولی بدون اضطراب |
| **کنتراست** | سبز ۲٫۵۴:۱ ❌ | سبز ۵٫۳۲:۱ ✅ |
| **نتیجه** | نوار پیشرفت ساده + درصد | حلقهٔ پل + درصد + دکمه‌های ادامه مسیر |
| **خطای ثبت** | بی‌صدا `console.error` — کاربر نمی‌فهمید امتیازش ثبت نشده | نوار خطا: «نتیجه ثبت نشد… پاسخ‌ها درست نمایش داده شده‌اند» + تلاش دوباره |
| **حرکت** | — | نبض ملایم برای درست، لرزش ۴px برای غلط (با احترام به `reduced-motion`) |

فایل‌ها: `shots/before-lesson.png` ↔ `shots/after-lesson.png`, `shots/v-crop-feedback.png`

### 📈 پیشرفت / پروفایل

| | قبل | بعد |
|---|---|---|
| **کنتراست بج‌ها** | `emerald-100/700`، `amber-100/700` — چند مورد زیر حد | همه به توکن‌های سمانتیک AA منتقل شد |
| **نشان‌ها** | اسپینر تک‌خطی «در حال بارگذاری نشان‌ها…»؛ خطا = ناپدید شدن بی‌صدای کل بخش | اسکلتون هم‌شکل قفسه (عنوان + نوار + ۶ کاشی) و حالت خطا با تلاش دوباره |
| **رنگ تیرها** | `#b45309`/`#475569`/`#6d28d9` | مقادیر بررسی‌شده (۷٫۶۸ / ۸٫۴۱ / ۱۰٫۰۲) |
| **فلش هدف** | `←` ثابت در متن | حذف شد |

فایل‌ها: `shots/before-progress.png` ↔ `shots/after-progress.png`

### 📱 موبایل — مهم‌ترین تغییر

| قبل | بعد |
|---|---|
| ۹ آیتم منو در نوار **بالای** صفحه با اسکرول افقی؛ آیتم‌های پنهان فقط با کشیدن پیدا می‌شدند؛ با یک دست غیرقابل استفاده | نوار **پایین** ثابت با ۵ خانه در دسترس شست؛ بقیه در شیت «بیشتر» با کاشی‌های بزرگ |

فایل‌ها: `shots/before-mobile-dashboard.png` ↔ `shots/v-mobile-top.png`, `shots/v-mobile-more.png`

---

## ۳. اعداد

| سنجه | قبل | بعد |
|---|---|---|
| قواعد `focus-visible` | **۰** | قاعده سراسری روی همه عناصر تعاملی |
| کنتراست `--muted` | ۴٫۴۸:۱ ❌ | ۵٫۷۴:۱ ✅ |
| کنتراست رنگ موفقیت | ۲٫۵۴:۱ ❌ | ۵٫۳۲:۱ ✅ |
| بازخورد فقط با رنگ | ۲ کامپوننت | ۰ |
| margin/alignment فیزیکی | ۲۴ مورد | ۰ |
| کامپوننت بدون حالت خطا | ۹ | ۰ در مسیرهای اصلی |
| ارتفاع دکمه | ۳۸px | ۴۴px |
| آیتم منوی موبایل | ۹ (اسکرول افقی، بالا) | ۵ (ثابت، پایین) |
| تست‌ها | ۶۷۲ ✅ | ۶۷۲ ✅ (unit ۲۹۱ · rls ۲۰۶ · prod ۸۸ · pwa ۸۷) |

هیچ API route، سیاست RLS، یا ساختار جدولی تغییر نکرد.

---

## ۴. ✅ چک‌لیست تست دستی

حساب آزمایشی: `demo@zabanyar.app` / `Zabanyar1404`

### 📱 موبایل (گوشی واقعی، نه فقط DevTools)

- [ ] **نوار پایین** — در همه صفحات دیده می‌شود، با شست راحت می‌رسی، آیتم فعال فیروزه‌ای و پررنگ است (با نقطه زیر آیکون)
- [ ] **شیت «بیشتر»** — باز می‌شود، ۵–۷ کاشی نشان می‌دهد، با ضربه بیرون یا Escape بسته می‌شود
- [ ] **پوشیده نشدن محتوا** — تا انتهای هر صفحه اسکرول کن؛ آخرین دکمه/متن **زیر نوار پایین پنهان نشود** (مخصوصاً داشبورد، درس، تکالیف)
- [ ] **توست نشان** — بعد از کسب نشان، بالای نوار پایین بنشیند نه پشت آن
- [ ] **بنر نصب PWA** — همینطور
- [ ] **آیفون با ناچ** — فاصله پایین (safe-area) درست باشد
- [ ] **چرخش صفحه** — در حالت افقی هم نوار پایین درست بماند
- [ ] **هدف لمسی** — هیچ دکمه‌ای آنقدر کوچک نباشد که خطا بزنی
- [ ] **صفحه درس** — بعد از «بررسی پاسخ‌ها»، ✓ و ✗ کنار گزینه‌ها دیده شود (نه فقط رنگ)

### 💻 دسکتاپ

- [ ] **ناوبری بالا** — ۴ آیتم + «بیشتر»؛ منوی «بیشتر» با کلیک بیرون بسته شود
- [ ] **hero داشبورد** — آمار هفته سمت چپ hero دیده شود، فضای خالی نماند
- [ ] **پیمایش کیبورد** — از ابتدای صفحه Tab بزن:
  - اولین توقف: «رفتن به محتوای اصلی» (skip link) باید ظاهر شود
  - **روی هر عنصر، حلقه فوکوس فیروزه‌ای دیده شود** — هیچ عنصری بی‌نشان نپرد
  - روی دکمه‌های رنگی هم حلقه دیده شود
  - Escape منوها را ببندد
- [ ] **صفحه ۱۲۸۰px و ۱۴۴۰px** — کارت‌ها کج و کوله نشوند

### 🎨 بصری

- [ ] رنگ اصلی همه‌جا فیروزه‌ای است — **هیچ آبی `#1d5cf5` باقی نمانده باشد**
- [ ] پاسخ غلط **آجری** است نه قرمز جیغ
- [ ] متن‌های خاکستری ثانویه به‌راحتی خوانده شوند
- [ ] فونت وزیرمتن در همه صفحات لود شود
- [ ] اعداد (استریک، XP، تایمر) موقع تغییر **نلرزند**

### 🔄 حالت‌ها

- [ ] **حالت خالی** — با یک حساب کاملاً جدید ثبت‌نام کن: داشبورد، درس‌ها و تکالیف باید پیام دعوت‌کننده + دکمه نشان دهند، نه یک خط خاکستری
- [ ] **حالت خطا** — DevTools → Network → Offline، بعد داشبورد را رفرش کن:
  - پنل مربی باید بگوید «تحلیل مربی هوشمند بارگذاری نشد — اتصال اینترنت را بررسی کن» + دکمه 🔄
  - در چت مربی پیام بفرست: باید نوار خطای سیستمی با «ارسال دوباره» بیاید، **نه پیامی از زبان مربی**
- [ ] **حالت لودینگ** — با throttling کند: اسکلتون باید **هم‌شکل محتوای واقعی** باشد (فهرست شبیه فهرست، چت شبیه چت)
- [ ] **نشانگر تایپ مربی** — سه نقطه بالا-پایین موقع انتظار پاسخ

### ♿ دسترس‌پذیری

- [ ] **کاهش حرکت** — سیستم‌عامل → Reduce Motion را روشن کن؛ shimmer، لرزش و pop باید متوقف شوند
- [ ] **زوم ۲۰۰٪** — متن نریزد بیرون
- [ ] **صفحه‌خوان** (اختیاری) — روی گزینه پاسخ، «پاسخ درست» یا «پاسخ شما — نادرست» خوانده شود

### 📲 اپ اندروید (TWA)

- [ ] چون TWA محتوا را زنده از سایت می‌گیرد، **تغییرات باید بدون نصب مجدد دیده شوند**. اپ نصب‌شده را باز کن و تأیید کن.
- [ ] رنگ نوار وضعیت باید فیروزه‌ای شود (`theme_color`). اگر نشد، یک‌بار اپ را ببند و باز کن.

---

## ۵. آنچه تغییر نکرد

- هیچ API route، سیاست RLS، یا ساختار جدولی
- منطق کسب‌وکار (SM-2، امتیازدهی، تعیین سطح تطبیقی، موتور AI)
- فونت وزیرمتن
- ساختار مسیرها

## ۶. پیشنهاد برای مرحله بعد

۱. **دارک‌مود کامل** — الان فقط ۵ متغیر سطح عوض می‌شود؛ نسخه کامل نیاز به مقیاس رنگ اختصاصی دارد
۲. **کلید دستی تم** (روشن/تاریک/سیستم)
۳. **آیکون SVG به‌جای ایموجی** — رندر ایموجی بین اندروید/iOS/ویندوز فرق دارد
۴. **APK جدید** — کلید امضای قبلی گم شده؛ نسخه بعدی به کلید جدید و نصب مجدد نیاز دارد

</div>
