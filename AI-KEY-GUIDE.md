<div dir="rtl">

# 🤖 کلید هوش مصنوعی — چیست و از کجا بگیرم؟

## کلید چیست؟

یک **رشته متنی** است که به سرویس‌دهنده هوش مصنوعی می‌گوید «این درخواست از طرف من است». چیزی شبیه این:

</div>

```
AIzaSyD8f3kL...
gsk_7xK2mN9pQ...
sk-proj-abc123...
```

<div dir="rtl">

آن را در تنظیمات Vercel می‌گذارید و سایت از آن به بعد، به‌جای موتور محلی، از مدل زبانی واقعی استفاده می‌کند.

---

## آیا اصلاً لازم است؟

**نه، اجباری نیست.** سایت همین حالا کامل کار می‌کند. تفاوت این است:

| بخش | الان (موتور محلی) | با کلید |
|---|---|---|
| تشخیص و تصحیح اشتباهات | ✅ دقیق (۲۴ قاعده) | ✅ دقیق‌تر و منعطف‌تر |
| آزمون تعیین سطح | ✅ کامل | ✅ همان (تغییری نمی‌کند) |
| مرور لغات SM-2 | ✅ کامل | ✅ همان (تغییری نمی‌کند) |
| **متن پاسخ‌های مکالمه** | ⚠️ از الگوهای ثابت — ممکن است تکراری شود | ✅ کاملاً طبیعی و پویا |
| **محتوای درس** | ⚠️ ۴ قالب آماده | ✅ هر موضوعی، بی‌نهایت تنوع |
| **بازخورد نوشتاری** | ⚠️ بر پایه قواعد | ✅ تحلیل عمیق سبک و انسجام |

> خلاصه: **مکالمه و تولید درس** بیشترین بهبود را می‌گیرند.

---

# 🏆 پیشنهاد من: Google Gemini

**چرا؟** سخاوتمندترین سرویس رایگان است، **بدون نیاز به کارت بانکی**، و برای فارسی هم خوب کار می‌کند.

| ویژگی | مقدار |
|---|---|
| هزینه | رایگان و دائمی |
| کارت بانکی | ❌ لازم نیست |
| محدودیت | حدود ۱٬۰۰۰ تا ۱٬۵۰۰ درخواست در روز |
| مدل | `gemini-2.5-flash` |

## مراحل گرفتن کلید (۲ دقیقه)

۱. به این آدرس بروید: **https://aistudio.google.com/apikey**
۲. با حساب گوگل خود وارد شوید
۳. روی **Create API key** بزنید
۴. **Create API key in new project** را انتخاب کنید
۵. کلید ساخته‌شده را کپی کنید (با `AIza` شروع می‌شود)

## مقادیری که باید در Vercel بگذارید

</div>

```
OPENAI_API_KEY   = AIzaSy...(کلید خودتان)
OPENAI_MODEL     = gemini-2.5-flash
OPENAI_BASE_URL  = https://generativelanguage.googleapis.com/v1beta/openai
```

<div dir="rtl">

> ⚠️ نام متغیر `OPENAI_API_KEY` است ولی کلید گوگل را در آن می‌گذارید — این طبیعی است. کد ما با هر سرویس **سازگار با OpenAI** کار می‌کند و گوگل هم چنین درگاهی دارد.

---

# ⚡ گزینه دوم: Groq (خیلی سریع)

اگر می‌خواهید مکالمه **فوری** جواب بدهد (کمتر از یک ثانیه)، Groq بهترین است.

| ویژگی | مقدار |
|---|---|
| هزینه | رایگان |
| کارت بانکی | ❌ لازم نیست |
| محدودیت | ۱٬۰۰۰ درخواست در روز · ۳۰ در دقیقه |
| سرعت | فوق‌العاده سریع |

**مراحل:** به **https://console.groq.com/keys** بروید → ثبت‌نام → **Create API Key** → کپی (با `gsk_` شروع می‌شود)

</div>

```
OPENAI_API_KEY   = gsk_...(کلید خودتان)
OPENAI_MODEL     = llama-3.3-70b-versatile
OPENAI_BASE_URL  = https://api.groq.com/openai/v1
```

<div dir="rtl">

---

# 💳 گزینه سوم: OpenAI (پولی)

بهترین کیفیت برای فارسی، اما **نیاز به کارت بانکی بین‌المللی** دارد که از ایران معمولاً مشکل‌ساز است.

**مراحل:** **https://platform.openai.com/api-keys** → ثبت‌نام → افزودن اعتبار (حداقل ۵ دلار) → **Create new secret key**

</div>

```
OPENAI_API_KEY   = sk-proj-...(کلید خودتان)
OPENAI_MODEL     = gpt-4o-mini
OPENAI_BASE_URL  = https://api.openai.com/v1
```

<div dir="rtl">

> 💰 هزینه واقعی برای این پروژه: با `gpt-4o-mini` هر ۱۰۰۰ مکالمه حدود **۰.۳ تا ۰.۵ دلار**. یعنی ۵ دلار برای ماه‌ها تست کافی است.

---

## 🇮🇷 نکته مهم برای دسترسی از ایران

هر سه سرویس ممکن است از IP ایران مسدود باشند و هنگام **گرفتن کلید** به تغییر IP نیاز داشته باشید.

اما نکته کلیدی این است:

> ✅ **بعد از اینکه کلید را در Vercel گذاشتید، دیگر مشکلی نیست.**
> درخواست‌ها از سرور Vercel در اروپا ارسال می‌شوند، نه از کامپیوتر شما. پس کاربران ایرانی سایت هیچ محدودیتی نخواهند داشت.

پس فقط برای همان ۲ دقیقهٔ ساخت کلید ممکن است به تغییر IP نیاز داشته باشید.

---

# 📝 چطور کلید را در سایت بگذارم؟

۱. به این آدرس بروید:
   **https://vercel.com/yaserrezai-7853/zabanyar/settings/environment-variables**

۲. سه متغیر را یکی‌یکی اضافه کنید (دکمه **Add Another**):

| Key | Value |
|---|---|
| `OPENAI_API_KEY` | کلید خودتان |
| `OPENAI_MODEL` | مثلاً `gemini-2.5-flash` |
| `OPENAI_BASE_URL` | آدرس درگاه سرویس |

۳. برای هر سه، هر سه محیط (**Production**، **Preview**، **Development**) را تیک بزنید

۴. **Save** بزنید

۵. به تب **Deployments** بروید → روی آخرین مورد سه‌نقطه (⋯) بزنید → **Redeploy**

---

## ✅ چطور بفهمم کار کرد؟

بعد از Redeploy، این آدرس را باز کنید:

**https://zabanyar-seven.vercel.app/api/health**

اگر درست باشد، این را می‌بینید:

</div>

```json
{
  "ai": {
    "mode": "provider",
    "host": "generativelanguage.googleapis.com",
    "model": "gemini-2.5-flash"
  }
}
```

<div dir="rtl">

اگر هنوز `"mode": "local-engine"` نوشته باشد، یعنی متغیرها ذخیره نشده‌اند یا Redeploy انجام نشده است.

**تست عملی:** به بخش **💬 مربی هوشمند** بروید و چند پیام بفرستید — پاسخ‌ها باید کاملاً طبیعی و متفاوت باشند، نه از الگوهای تکراری.

---

## 🛡️ اگر کلید کار نکرد چه می‌شود؟

**هیچ اتفاق بدی نمی‌افتد.** کد طوری نوشته شده که اگر سرویس خطا بدهد، در دسترس نباشد، یا سقف رایگان تمام شود، **خودکار به موتور محلی برمی‌گردد**. سایت هرگز از کار نمی‌افتد و کاربر خطا نمی‌بیند.

</div>
