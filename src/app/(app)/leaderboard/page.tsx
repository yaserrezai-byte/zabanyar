import Leaderboard from '@/components/Leaderboard';

export const metadata = { title: 'جدول امتیاز | زبان‌یار' };
export const dynamic = 'force-dynamic';

export default function LeaderboardPage() {
  return <Leaderboard />;
}
