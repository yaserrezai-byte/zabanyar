<div dir="rtl">

# 🌍 معماری چندزبانه زبان‌یار

> نسخه ۱٫۰ · commit `908e728` · مهاجرت `0010_multi_language.sql`
> وضعیت: **انگلیسی 🇬🇧 + اسپانیایی 🇪🇸 فعال روی سایت**

---

## ۱. مدل کار

کاربر **اول زبان را انتخاب می‌کند**، بعد وارد مسیر مستقل آن زبان می‌شود. هر زبان سطح، لغات، اشتباهات، درس‌ها و استریک **کاملاً جدا** دارد. هر لحظه می‌شود بین دو مسیر جابه‌جا شد و هیچ داده‌ای از دست نمی‌رود.

```
ورود → /languages (انتخاب زبان)
          ├── انگلیسی → آزمون تعیین سطح EN → داشبورد EN
          └── اسپانیایی → آزمون تعیین سطح ES → داشبورد ES
```

---

## ۲. مشکلی که باید اول حل می‌شد

هیچ‌کدام از ۲۰ جدول ستون «زبان» نداشتند. بدتر، **سه قید یکتایی** با دو زبان مستقیماً تصادم می‌کردند:

| جدول | قید قدیم | چه اتفاقی می‌افتاد |
|---|---|---|
| `skill_levels` | `unique(user_id, skill)` | «گرامر» انگلیسی و اسپانیایی یک ردیف می‌شدند — سطح‌ها روی هم می‌ریختند |
| `vocabulary_memory` | `unique(user_id, word)` | `actual` در هر دو زبان هست (و معنی متفاوت دارد) — تصادم |
| `mistakes_memory` | `unique(user_id, error_tag)` | `article` در هر دو زبان هست — آمار خطا قاطی می‌شد |

پس برخلاف کارهای قبلی، این بار **تغییر schema اجتناب‌ناپذیر بود**.

### مهاجرت امن

- هر ستون جدید `NOT NULL DEFAULT 'en'` → همه ردیف‌های موجود سالم می‌مانند و به انگلیسی نسبت داده می‌شوند
- قید جدید **قبل از** حذف قید قدیم ساخته می‌شود → هیچ لحظه‌ای بدون محافظت یکتایی نمی‌مانیم
- کل فایل idempotent است و دوباره اجرا شدنش مشکلی ندارد

**تأیید پس از اجرا روی دیتابیس واقعی:**
```
قیدها:      skill_levels_user_id_language_skill_key ✅
            vocabulary_memory_user_id_language_word_key ✅
            mistakes_memory_user_id_language_error_tag_key ✅
ستون زبان:  ۱۳ جدول ✅
backfill:   ۵۷ کاربر موجود → انگلیسی ✅
RLS:        فعال روی user_languages ✅
```

---

## ۳. ساختار داده

**جدول جدید `user_languages`** — یک ردیف به ازای (کاربر، زبان):

| ستون | نقش |
|---|---|
| `current_level` / `target_level` | سطح CEFR **همان زبان** |
| `placement_done` | آزمون تعیین سطح این زبان انجام شده؟ |
| `streak_days` / `last_active_on` | استریک مستقل هر زبان |

**`profiles.active_language`** فقط نشان می‌دهد کاربر الان روی کدام مسیر است.

> نکته: `profiles.current_level` و `placement_done` به‌عنوان «آینه» زبان فعال به‌روز می‌مانند تا پنل مدرس و کدهای قدیمی نشکنند.

---

## ۴. جداسازی در لایه کد

نقطه کلیدی `buildLearnerContext(supabase, userId, language)` است: **همه** خواندن‌های داده کاربر با `language` فیلتر می‌شوند. یعنی یک ضعف انگلیسی هرگز وارد درس اسپانیایی نمی‌شود.

| فایل | نقش |
|---|---|
| `src/lib/languages.ts` | رجیستری زبان‌ها — تنها منبع حقیقت |
| `src/lib/active-language.ts` | تشخیص زبان فعال + ساخت خودکار مسیر |
| `src/lib/ai/banks.ts` | dispatch محتوا بر اساس زبان |

**افزودن زبان سوم** باید یعنی: یک ورودی در `languages.ts` + یک بانک محتوا. صفحات دست نمی‌خورند.

---

## ۵. محتوای اسپانیایی

محتوا **ترجمه بانک انگلیسی نیست**. روی چیزهایی تمرکز دارد که یک فارسی‌زبان واقعاً در اسپانیایی اشتباه می‌کند:

| مشکل | چرا برای فارسی‌زبان سخت است |
|---|---|
| جنسیت اسم (`el/la`) | فارسی اصلاً جنسیت دستوری ندارد |
| `ser` در برابر `estar` | فارسی فقط یک «بودن» دارد |
| صرف فعل | شش شخص، بسیار سنگین‌تر از فارسی |
| وجه التزامی | معادل مستقیم فارسی ندارد |
| `por` / `para` | هر دو در فارسی «برای» می‌شوند |
| ساختار `gustar` | وارونه است: «برای من خوشایند است» |

**حجم:** ۳۸ سؤال تعیین سطح (A1–C2) · ۱۰ قاعده تشخیص خطا · ۳ قالب درس · ۲۴ جمله تلفظ

**تلفظ:** جمله‌ها حول صداهای سخت انتخاب شده‌اند — `rr` غلتان، `j`≈«خ»، `c/z`=/θ/، `ñ`، و اینکه `b` و `v` یک صدا دارند.

---

## ۶. صدا و لهجه

| زبان | لوکال | لهجه |
|---|---|---|
| انگلیسی | `en-US` | آمریکایی |
| اسپانیایی | `es-ES` | کاستیلی (اسپانیای اروپا) |

اسپانیایی کاستیلی انتخاب شد چون بانک سؤال از تمایز `/θ/` («ceceo») استفاده می‌کند و باید با چیزی که شنیده می‌شود هم‌خوان باشد.

---

## ۷. کار با کلید AI

**الان کلید AI تنظیم نیست** (`OPENAI_API_KEY` خالی است). به همین دلیل اسپانیایی طوری ساخته شد که **بدون کلید هم کار کند** — دقیقاً مثل انگلیسی، با موتور محلی.

| قابلیت | بدون کلید | با کلید |
|---|---|---|
| تعیین سطح | ✅ بانک ۳۸ سؤالی | ✅ همان |
| درس | ✅ ۳ قالب | ✅ نامحدود و شخصی‌سازی‌شده |
| مربی گفت‌وگو | ✅ پاسخ قاعده‌محور | ✅ مکالمه واقعی |
| تصحیح نوشتار | ✅ ۱۰ قاعده | ✅ تحلیل کامل |
| تلفظ | ✅ کامل | ✅ + Whisper |

وقتی کلید را اضافه کنید، همه پرامپت‌ها **از قبل زبان‌آگاه هستند** و صراحتاً می‌گویند به کدام زبان پاسخ داده شود؛ هیچ تغییر کدی لازم نیست.

---

## ۸. تست

**۶۹۳ → ۷۲۷ تست** (‎+۳۴)

بخش «۱۹) Spanish track» این‌ها را می‌سنجد: پوشش کامل CEFR، یکتا بودن شناسه‌ها، عدم تصادم با بانک انگلیسی، حضور پداگوژی مخصوص اسپانیایی، **توزیع عادلانه پاسخ‌ها** (همان باگ «همه جواب‌ها B»)، تشخیص درست خطا و نبود false positive روی متن سالم.

**تأیید انتها-به-انتها روی سرور واقعی:**
```
۱. زبان پیش‌فرض: en
۲. سوییچ به es → placement_done: false (مسیر جدا) ✅
۳. سؤال اسپانیایی: «Voy a ___ una decisión.» ✅
۴. آزمون اسپانیایی تمام شد → سطح A2
۵. برگشت به en → placement_done همچنان false ✅
۶. دیتابیس: skill_levels {"es": 6} — انگلیسی دست‌نخورده ✅
```

---

## ۹. ✅ چک‌لیست تست دستی

- [ ] **انتخاب اولیه** — با حساب جدید ثبت‌نام کن؛ `/languages` هر دو زبان را نشان دهد
- [ ] **مسیر جدا** — اسپانیایی را انتخاب کن، آزمون بده. بعد به انگلیسی برگرد: باید **دوباره** آزمون بخواهد
- [ ] **سوییچ سریع** — از تراشه زبان در نوار بالا بین دو زبان جابه‌جا شو؛ سطح و استریک باید عوض شوند
- [ ] **جداسازی لغات** — در هر زبان چند لغت بساز؛ صفحه مرور لغات هر زبان فقط لغات خودش را نشان دهد
- [ ] **تلفظ** — در اسپانیایی روی 🔈 بزن؛ باید صدای اسپانیایی بشنوی نه انگلیسی
- [ ] **صفحه تلفظ** — جمله‌ها باید اسپانیایی باشند و «لهجه اسپانیای اروپا» نوشته شده باشد
- [ ] **مربی** — در اسپانیایی، placeholder باید «Escribe en español…» باشد
- [ ] **موبایل** — کارت‌های انتخاب زبان زیر نوار پایین پنهان نشوند

---

## ۱۰. آنچه هنوز نیست

۱. **گفت‌وگوی گروهی اسپانیایی** — ستون `language` اضافه و ایندکس matchmaking اصلاح شده، ولی سناریوهای گروهی هنوز انگلیسی‌اند
۲. **پنل مدرس** — هنوز زبان‌آگاه نیست؛ مدرس همه زبان‌های دانش‌آموز را یکجا می‌بیند
۳. **درس‌های اسپانیایی بیشتر** — ۳ قالب محلی داریم؛ با کلید AI عملاً نامحدود می‌شود
۴. **نشان‌ها** — فعلاً بین دو زبان مشترک‌اند (قابل بحث: شاید بهتر باشد همین‌طور بماند)

</div>
