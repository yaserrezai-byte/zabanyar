<div dir="rtl">

# 🧪 راهنمای تست زبان‌یار

فقط مرورگرتان را باز کنید — نیازی به نصب هیچ چیزی نیست.

## 🔗 آدرس سایت

# [zabanyar-seven.vercel.app](https://zabanyar-seven.vercel.app)

---

## 🔑 حساب‌های آماده

دو حساب برایتان ساخته‌ام. می‌توانید مستقیماً وارد شوید:

| نقش | ایمیل | رمز عبور |
|---|---|---|
| 👤 **زبان‌آموز** | `demo@zabanyar.app` | `Zabanyar1404` |
| 🛡️ **مدیر** | `admin@zabanyar.app` | `Zabanyar1404` |

> یا می‌توانید با ایمیل خودتان **ثبت‌نام** کنید — تأیید ایمیل غیرفعال است، پس بلافاصله وارد می‌شوید.

---

# 📋 مسیر تست پیشنهادی (حدود ۱۰ دقیقه)

## قدم ۱ — ورود و تنظیمات اولیه

با `demo@zabanyar.app` وارد شوید. یک ویزارد ۴ مرحله‌ای می‌بینید:

۱. **نام** خود را وارد کنید
۲. **هدف روزانه** را انتخاب کنید (مثلاً ۲۰ دقیقه)
۳. **سطح هدف** را انتخاب کنید (مثلاً B2)
۴. **علاقه‌مندی‌ها** را انتخاب کنید — مثلاً «سفر» و «فیلم و سریال»

> 🔍 **این را امتحان کنید:** علاقه‌مندی‌هایتان را یادداشت کنید. بعداً در بخش مربی هوشمند خواهید دید که موضوع گفت‌وگو دقیقاً بر اساس همین‌ها انتخاب می‌شود.

---

## قدم ۲ — آزمون تعیین سطح (مهم‌ترین بخش)

بعد از ویزارد، مستقیماً به آزمون می‌روید. ۱۴ سؤال.

> 🔍 **این را حتماً امتحان کنید:** چند سؤال اول را **عمداً درست** جواب دهید و به گوشه بالا-چپ کادر نگاه کنید (برچسب `A2`، `B1`، `B2`…). خواهید دید که **سطح سؤال‌ها بالا می‌رود**.
>
> بعد چند سؤال را **عمداً غلط** جواب دهید — سطح دوباره **پایین می‌آید**. این همان آزمون تطبیقی است؛ سؤال‌ها ثابت نیستند.

بعد از هر پاسخ، جواب درست سبز و توضیح فارسی نمایش داده می‌شود.

در پایان: سطح CEFR شما + نمودار امتیاز هر مهارت.

---

## قدم ۳ — داشبورد

حالا داشبورد کامل را می‌بینید:

- 🧠 **کارت آبی مربی هوشمند** — تحلیل وضعیت شما و ۳ کار پیشنهادی برای امروز
- 📊 **نقشه راداری مهارت‌ها** — بر اساس نتیجه آزمون
- 🔍 **تحلیل اشتباهات** (ستون کناری) — اینجا مهم‌ترین قابلیت است

> 🔍 **این را امتحان کنید:** در بخش «تحلیل اشتباهات» می‌بینید که سیستم دقیقاً فهمیده در چه چیزهایی اشتباه کردید. روی دکمه **«درس اختصاصی بساز»** زیر یکی از آن‌ها بزنید — یک درس کامل مخصوص همان ضعف شما ساخته می‌شود.

---

## قدم ۴ — کارگاه نوشتن (بهترین بخش برای تست)

از منوی بالا: **✍️ تکالیف**

این متن را **عیناً کپی کنید** داخل کادر:

</div>

```
yesterday i buyed a book and i readed it. it was very intresting but i didn't saw nobody at the shop.
```

<div dir="rtl">

روی **«🔍 تصحیح و نمره‌دهی»** بزنید.

باید این ۶ اشتباه را بگیرد:

| اشتباه | اصلاح | نوع |
|---|---|---|
| `buyed` | `bought` | فعل بی‌قاعده |
| `readed` | `read` | فعل بی‌قاعده |
| `intresting` | `interesting` | املا |
| `didn't saw` | `didn't see` | گذشته ساده |
| `didn't see nobody` | `didn't see anybody` | منفی مضاعف |
| `yesterday i` | `Yesterday I` | حرف بزرگ |

> 🔍 **نکته مهم:** پایین صفحه نوشته «این اشتباهات به حافظه هوشمند اضافه شدند». حالا به **داشبورد** برگردید — می‌بینید که این اشتباهات جدید در بخش «تحلیل اشتباهات» ظاهر شده‌اند. **حافظه سیستم واقعاً کار می‌کند.**

متن خودتان را هم امتحان کنید — هر چیزی به انگلیسی بنویسید.

---

## قدم ۵ — مربی هوشمند

از منو: **💬 مربی هوشمند**

این جمله را بفرستید (عمداً غلط است):

</div>

```
Yesterday I go to the park and I am agree it was nice
```

<div dir="rtl">

مربی جواب می‌دهد و زیر پاسخش، کادر زرد **«✏️ تصحیح‌ها»** را نشان می‌دهد با توضیح فارسی هر اشتباه.

> 🔍 **این را امتحان کنید:**
> - روی **«نمایش ترجمه»** بزنید تا ترجمه فارسی پاسخ را ببینید
> - از ستون راست یک **سناریو** انتخاب کنید (مثلاً ☕ کافی‌شاپ یا 💼 مصاحبه شغلی) و دوباره چت کنید — لحن و موضوع عوض می‌شود

---

## قدم ۶ — درس‌ها

از منو: **📚 درس‌ها** ← دکمه **«✨ ساخت درس جدید»**

یک درس کامل ساخته می‌شود:
- ۳ بخش آموزشی با توضیح فارسی
- مثال‌های انگلیسی + ترجمه
- نکات کلیدی
- ۶ واژه با معنی فارسی
- ۶ تمرین چهارگزینه‌ای

تمرین‌ها را جواب دهید و **«بررسی پاسخ‌ها»** بزنید — نمره و توضیح هر سؤال را می‌بینید.

---

## قدم ۷ — مرور لغات

از منو: **🔁 مرور لغات**

لغت‌هایی که از درس‌ها آمده‌اند اینجا هستند. روی **«نمایش معنی»** بزنید و بعد یکی از چهار گزینه را انتخاب کنید:

😰 بلد نبودم · 😅 سخت بود · 🙂 خوب بود · 😎 خیلی راحت

> 🔍 **این را امتحان کنید:** روی یک لغت «😎 خیلی راحت» بزنید. آن لغت تا ۶ روز دیگر نشان داده نمی‌شود. اگر «😰 بلد نبودم» بزنید، فردا دوباره می‌آید. این الگوریتم SM-2 است.

---

## قدم ۸ — گزارش پیشرفت

از منو: **📈 پیشرفت**

نمودار فعالیت ۳۰ روزه، رادار مهارت‌ها، و فهرست کامل الگوهای خطای شما با میزان شدت هر کدام.

---

## قدم ۹ — پنل مدیریت

**خروج** بزنید و با `admin@zabanyar.app` وارد شوید.

حالا در منوی بالا **🛡️ مدیریت** ظاهر می‌شود: آمار کل سامانه و فهرست کاربران.

> 🔍 **تست امنیت:** دوباره با حساب `demo@zabanyar.app` وارد شوید و آدرس زیر را مستقیماً در مرورگر باز کنید:
>
> `https://zabanyar-seven.vercel.app/admin`
>
> پیام **«🔒 دسترسی محدود»** می‌بینید. این محدودیت در سطح **دیتابیس** اعمال می‌شود، نه فقط در ظاهر سایت — یعنی حتی با دستکاری مرورگر هم قابل دور زدن نیست.

---

## 📱 تست موبایل

همین آدرس را با گوشی باز کنید. تمام صفحات واکنش‌گرا هستند و منو به‌صورت نوار افقی قابل اسکرول نمایش داده می‌شود.

---

# 📱 تست PWA (نصب روی گوشی و کار آفلاین)

<div dir="rtl">

زبان‌یار حالا یک **PWA کامل** است: می‌توانید آن را مثل یک اپلیکیشن واقعی روی گوشی نصب کنید و شِل آن حتی بدون اینترنت باز می‌شود.

## ✅ چک‌لیست سریع (نسخه زنده)

| بررسی | چطور | نتیجه مورد انتظار |
|---|---|---|
| مانیفست | باز کردن `/manifest.json` | JSON با نام «زبان‌یار»، `dir: "rtl"`، `lang: "fa"` |
| سرویس‌ورکر | باز کردن `/sw.js` | فایل جاوااسکریپت (نه ریدایرکت به `/login`) |
| صفحه آفلاین | باز کردن `/offline` | پیام «اتصال اینترنت برقرار نیست» |
| آیکون | باز کردن `/icons/icon-192.png` | آیکون آبی با حرف «ز» |

## 🖥 تست با Chrome DevTools

۱. سایت را در **کروم دسکتاپ** باز کنید و `F12` را بزنید.
۲. تب **Application** → بخش **Manifest**:
   - نام، آیکون‌ها و رنگ‌ها باید نمایش داده شوند.
   - در بالای پنل نباید هیچ خطای قرمزی باشد.
۳. بخش **Service Workers**:
   - باید یک ورکر با وضعیت **activated and is running** ببینید.
۴. بخش **Cache Storage**:
   - باید `zabanyar-shell-v1` و `zabanyar-static-v1` وجود داشته باشند.
   - 🔒 **مهم:** داخل آن‌ها **نباید** هیچ آدرسی از `/api/`، `/auth`، `/login` یا `supabase.co` باشد.

### تست واقعی آفلاین
۵. در تب **Network**، حالت را روی **Offline** بگذارید.
۶. صفحه را رفرش کنید → باید صفحه «اتصال اینترنت برقرار نیست» ظاهر شود.
۷. به بخش «مرور لغات» بروید → باید پیام فارسی حالت آفلاین را ببینید، نه خطای خام.

## 📊 درباره امتیاز Lighthouse

> ⚠️ **نکته مهم:** از نسخه ۱۲ به بعد، **Lighthouse دسته‌بندی مجزای PWA را حذف کرده است**. بنابراین «امتیاز PWA بالای ۹۰» دیگر قابل اندازه‌گیری نیست و اگر جایی چنین امتیازی دیدید، مربوط به نسخه‌های قدیمی است.

معیار جایگزین و دقیق‌تر، **حکم خود کروم درباره قابل‌نصب بودن** است:

</div>

```bash
# اجرای Lighthouse روی نسخه محلی
npx lighthouse@12 http://localhost:3000/ \
  --only-categories=performance,accessibility,best-practices,seo \
  --chrome-flags="--headless=new --no-sandbox" --view
```

<div dir="rtl">

امتیازهای فعلی اندازه‌گیری‌شده روی بیلد پروडاکشن:

| دسته | امتیاز |
|---|---|
| Performance | ۸۶ |
| Accessibility | ۹۵ |
| Best Practices | ۱۰۰ |
| SEO | ۱۰۰ |

و برای تأیید قابل‌نصب بودن (جایگزین معیار قدیمی PWA):

**DevTools → Application → Manifest → دکمه «Install»** — اگر فعال بود، یعنی کروم اپ را قابل نصب می‌داند.

## 📲 نصب روی اندروید

۱. سایت را در **کروم اندروید** باز کنید.
۲. چند ثانیه صبر کنید → بنر فارسی **«نصب زبان‌یار»** پایین صفحه ظاهر می‌شود.
۳. روی **⬇️ نصب زبان‌یار** بزنید.
۴. یا از منوی سه‌نقطه → **Add to Home screen**.

**بعد از نصب بررسی کنید:**
- آیکون «ز» روی صفحه اصلی گوشی باشد.
- با باز کردن آن، **نوار آدرس مرورگر دیده نشود** (حالت standalone).
- نوار وضعیت گوشی آبی (`#1d5cf5`) شود.
- با فشار طولانی روی آیکون، سه میان‌بر ببینید: «مربی هوشمند»، «مرور لغات»، «تمرین تلفظ».

## 🍎 نصب روی آیفون (iOS)

> iOS از رویداد خودکار نصب پشتیبانی نمی‌کند، برای همین بنر ما راهنمای دستی نشان می‌دهد.

۱. سایت را حتماً در **Safari** باز کنید (کروم روی iOS این قابلیت را ندارد).
۲. دکمه **اشتراک‌گذاری** (مربع با فلش رو به بالا) را بزنید.
۳. **Add to Home Screen** را انتخاب کنید.
۴. نام «زبان‌یار» را تأیید کنید.

**بررسی کنید:** آیکون بدون حاشیه سفید باشد و اپ بدون نوار Safari باز شود.

## 🔒 تست امنیت کش (مهم)

طبق طراحی، **هیچ داده شخصی کاربر** در کش سرویس‌ورکر ذخیره نمی‌شود. برای اطمینان:

۱. وارد حساب شوید و چند صفحه (لغات، مربی، تکالیف) را باز کنید.
۲. DevTools → **Application → Cache Storage** را باز کنید.
۳. محتوای همه کش‌ها را بررسی کنید.

**نباید** هیچ‌کدام از این‌ها را ببینید:
- ❌ آدرسی شامل `supabase.co` (توکن و داده کاربر)
- ❌ آدرسی شامل `/api/` (پاسخ‌های شخصی مثل لغات و اشتباهات)
- ❌ `/login`، `/signup`، `/auth/...`
- ❌ صفحات شخصی مثل `/dashboard` یا `/teacher`

**باید فقط** این‌ها باشند: فایل‌های `/_next/static/`، آیکون‌ها، `/manifest.json`، `/offline` و صفحه اصلی عمومی.

## 🤖 تست خودکار

</div>

```bash
npm run test:pwa          # ۸۷ بررسی: مانیفست، آیکون‌ها، هدرها، قواعد امنیتی کش
```

<div dir="rtl">

این تست علاوه بر بررسی فایل‌ها، **توابع واقعی سرویس‌ورکر را اجرا می‌کند** و تأیید می‌کند که مسیرهای Supabase، احراز هویت و API هرگز کش نمی‌شوند.

## 🔄 پاک کردن کش هنگام مشکل

اگر بعد از یک بروزرسانی چیزی درست کار نکرد:

**DevTools → Application → Storage → Clear site data**

یا در کنسول مرورگر:

</div>

```js
navigator.serviceWorker.controller?.postMessage('CLEAR_CACHES');
caches.keys().then(k => k.forEach(c => caches.delete(c)));
location.reload();
```

<div dir="rtl">

</div>

---

# ❓ پرسش‌های احتمالی

<details>
<summary><b>چرا جواب‌های مربی گاهی تکراری است؟</b></summary>

چون هنوز کلید سرویس هوش مصنوعی وصل نشده و اپ روی **موتور محلی** کار می‌کند. تشخیص و تصحیح اشتباهات کاملاً دقیق است، اما متن پاسخ‌های مکالمه از الگوهای از پیش تعریف‌شده می‌آید.

برای فعال‌کردن پاسخ‌های کاملاً پویا، طبق راهنمای `DEPLOYMENT.md` یک `OPENAI_API_KEY` اضافه کنید. بقیه سیستم دست‌نخورده باقی می‌ماند.

حالت فعلی را اینجا ببینید: [/api/health](https://zabanyar-seven.vercel.app/api/health)
</details>

<details>
<summary><b>اولین بارگذاری کند است</b></summary>

پلن رایگان Vercel سرور را در حالت بی‌کاری می‌خواباند. اولین درخواست ۲ تا ۴ ثانیه طول می‌کشد؛ بعد از آن سریع است.
</details>

<details>
<summary><b>می‌خواهم داده‌های تست را پاک کنم و از صفر شروع کنم</b></summary>

کافی است با یک ایمیل جدید ثبت‌نام کنید. یا در Supabase → SQL Editor این را اجرا کنید:

```sql
delete from auth.users where email = 'demo@zabanyar.app';
```

تمام داده‌های مرتبط به‌صورت آبشاری پاک می‌شوند.
</details>

<details>
<summary><b>می‌خواهم حساب خودم را مدیر کنم</b></summary>

در Supabase → SQL Editor:

```sql
update public.profiles set role = 'admin' where email = 'ایمیل-شما';
```
</details>

</div>
