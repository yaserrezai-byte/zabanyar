<div dir="rtl">

# 🎨 سیستم طراحی زبان‌یار

> نسخه ۱٫۰ · مرداد ۱۴۰۵
> مرجع: [`UI-AUDIT.md`](./UI-AUDIT.md) · پیاده‌سازی: `src/app/globals.css` + `src/components/ui/`

این سند تصمیم‌های طراحی را **با دلیل** ثبت می‌کند. هر رنگ در این سند با فرمول WCAG محاسبه شده، نه حدس زده شده.

---

## ۱. اصول

| اصل | یعنی چه |
|---|---|
| **موبایل اول** | بیشتر زبان‌آموزان با گوشی تمرین می‌کنند. هر تصمیم اول در ۳۹۰px آزمایش می‌شود. |
| **یک کار در هر صفحه** | هر صفحه یک عنصر بصری غالب دارد که می‌گوید «الان این را انجام بده». بقیه ثانویه‌اند. |
| **دسترس‌پذیری شرط عبور است** | حداقل ۴٫۵:۱ کنتراست، فوکوس دیدنی، و هیچ اطلاعاتی فقط با رنگ منتقل نمی‌شود. |
| **لحن سیستم، نه لحن انسان** | خطا می‌گوید چه شد و کاربر چه کند. عذرخواهی نمی‌کند. |
| **حرکت کم و معنادار** | انیمیشن فقط جایی که چیزی را توضیح می‌دهد. |

---

## ۲. پالت رنگ و **چرا این رنگ**

### تصمیم اصلی: فیروزه‌ای ایرانی

اولین چیزی که به ذهن رسید آبی/سبز بود — همان پالتی که هر اپ آموزشی دیگری هم به آن می‌رسد. طبق خواسته بریف، این تصمیم **عوض شد**:

**رنگ اصلی = فیروزه‌ای کاشی ایرانی (`#0E7C8A`)**

چرا:
1. **ریشه فرهنگی** — فیروزه‌ای رنگ کاشی‌کاری مساجد و معماری ایرانی است. زبان‌یار محصولی برای **فارسی‌زبان‌ها**ست، نه یک اپ انگلیسی عمومی. رنگ باید این را بگوید.
2. **از پالت‌های تکراری فاصله دارد** — نه سبز/بنفش دولینگو، نه cream/terracotta، نه near-black/acid-green.
3. **از نظر روانی درست است** — فیروزه‌ای آرام و متمرکز است، نه هیجان‌زده. یادگیری زبان یک ماراتن است نه یک بازی کوتاه؛ رنگی که هر روز می‌بینی نباید خسته‌کننده یا پرفشار باشد.

**رنگ مکمل = زعفرانی (`#9E5A00`)** — گرم، ایرانی، و در کنار فیروزه‌ای همان تضاد کلاسیک کاشی‌کاری را می‌سازد. برای دستاورد، استریک و تأکید.

**پس‌زمینه = `#FBFAF7`** کاغذی گرم، نه خاکستری‌آبی بیمارستانی. با فیروزه‌ای و زعفرانی هم‌خانواده است.

### جدول کامل — همه با کنتراست محاسبه‌شده

| توکن | کد | روی `--bg` | روی سفید | سفید روی آن | کاربرد |
|---|---|---|---|---|---|
| `primary-600` | `#0E7C8A` | ۴٫۷۱ | ۴٫۹۲ ✅ | ۴٫۹۲ ✅ | دکمه اصلی، حلقه پیشرفت |
| `primary-700` | `#0A6270` | ۶٫۷۲ ✅ | ۷٫۰۱ ✅ | ۷٫۰۱ ✅ | متن روی سفید، hover |
| `primary-800` | `#084E59` | ۸٫۹۶ ✅ | ۹٫۳۶ ✅ | — | متن روی زمینه روشن برند |
| `accent-700` | `#9E5A00` | ۵٫۱۴ ✅ | ۵٫۳۶ ✅ | ۵٫۳۶ ✅ | زعفرانی — دستاورد |
| `success-700` | `#1F7A4D` | ۵٫۰۹ ✅ | ۵٫۳۲ ✅ | ۵٫۳۲ ✅ | پاسخ درست |
| `error-600` | `#B4453C` | ۵٫۲۲ ✅ | ۵٫۴۴ ✅ | ۵٫۴۴ ✅ | پاسخ غلط — **آجری، نه قرمز تند** |
| `warning-700` | `#8A6100` | ۵٫۳۱ ✅ | ۵٫۵۴ ✅ | — | هشدار |
| `info-700` | `#1C5A96` | ۶٫۸۱ ✅ | ۷٫۱۱ ✅ | — | توضیح خنثی |
| `--fg` | `#141A24` | ۱۶٫۷۲ ✅ | ۱۷٫۴۶ ✅ | — | متن اصلی |
| `--muted` | `#5A6473` | ۵٫۷۴ ✅ | ۵٫۹۹ ✅ | — | متن ثانویه |
| `--border` | `#E7E3DC` | — | — | — | جداکننده تزئینی |
| `--border-strong` | `#8B8272` | ۳٫۶۴ ✅ | ۳٫۷۹ ✅ | — | مرز عناصر تعاملی (استاندارد ۳:۱ برای UI) |

**۱۸ ترکیب متن‌روی‌زمینه‌رنگی** هم بررسی شد (مثل `success-800` روی `success-50` = ۶٫۷۵). همه قبول.

> **چرا خطا آجری است نه قرمز:** بریف صریحاً گفت «نه قرمز تند و اضطراب‌آور». `#B4453C` رنگ آجر است — خوانا، جدی، ولی زنگ خطر نمی‌زند. اشتباه در یادگیری زبان طبیعی است و رابط نباید آن را مثل یک خطای سیستمی نشان دهد.

### رنگ‌های حذف‌شده

| رنگ | نسبت | چرا رد شد |
|---|---|---|
| `#10b981` (سبز قبلی) | ۲٫۵۴:۱ | رد شدید WCAG — رنگ «موفقیت» قابل خواندن نبود |
| `#64748b` (muted قبلی) | ۴٫۴۸:۱ | زیر حد استاندارد |
| `#D98324` زعفرانی روشن | ۲٫۹۱:۱ | با متن سفید ناخواناست |
| `#B26A00` زعفرانی متوسط | ۴٫۲۴:۱ | فقط AA-large — کافی نیست |

---

## ۳. عنصر امضا: «حلقهٔ پل» (Bridge Ring)

**فایل:** `src/components/ui/BridgeRing.tsx`

یک **کمان باز** — نه دایره کامل — که:

- **از پایین‌راست شروع می‌شود** → جایی که خواندن فارسی آغاز می‌شود
- **از بالا می‌چرخد و به پایین‌چپ می‌رسد** → جایی که خواندن انگلیسی آغاز می‌شود
- **شکاف پایین باز می‌ماند** (۸۴ درجه) → دو «کرانه» پل

**چرا با موضوع زبان‌یار هم‌خوان است:**
یک progress ring معمولی دایره بسته است و فقط می‌گوید «چقدر تمام شد». حلقهٔ پل چیز دیگری می‌گوید: **تو در حال عبور از یک زبان به زبان دیگری هستی.** جهت پرشدن دقیقاً همان حرکتی است که مغز یک فارسی‌زبان هنگام یادگیری انگلیسی انجام می‌دهد — از راست‌چین به چپ‌چین. این تصمیم برای هیچ اپ آموزشی دیگری معنا نمی‌دهد؛ فقط برای پلی بین فارسی و انگلیسی.

**کجا تکرار می‌شود:** هدف روزانه (داشبورد)، نتیجه تمرین درس، سطح CEFR (آزمون تعیین سطح)، پایان مرور لغات.

---

## ۴. تایپوگرافی

فونت **وزیرمتن** حفظ شد — بهترین فونت فارسی متن‌باز برای رابط، با ارقام فارسی/لاتین هماهنگ.

| کلاس | اندازه (موبایل → دسکتاپ) | ارتفاع خط | وزن |
|---|---|---|---|
| `.t-display` | ۲rem → ۲٫۵rem | ۱٫۳۵ | ۷۰۰ |
| `.t-h1` | ۱٫۵rem → ۱٫۷۵rem | ۱٫۴۵ | ۷۰۰ |
| `.t-h2` | ۱٫۱۷۵rem | ۱٫۵ | ۷۰۰ |
| `.t-h3` | ۱rem | ۱٫۶ | ۵۰۰ |
| `.t-body` | ۰٫۹۵rem | **۱٫۹** | ۴۰۰ |
| `.t-caption` | ۰٫۸rem | ۱٫۷ | ۴۰۰ |

**ارتفاع خط بدنه ۱٫۹ است، نه ۱٫۵.** خط فارسی زیر-خط و بالانویس بیشتری دارد؛ فاصله استاندارد لاتین باعث تنگی می‌شود.

**اعداد:** کلاس `.num` با `font-variant-numeric: tabular-nums` — تا شمارنده‌ها (استریک، XP، تایمر) موقع تغییر نلرزند.

---

## ۵. فاصله، شعاع، سایه

**فاصله:** پایه ۴px (مقیاس پیش‌فرض تیلویند).

**شعاع گردی** — قبلاً بی‌نظم بود، حالا مقیاس دارد:

| توکن | مقدار | کاربرد |
|---|---|---|
| `--radius-sm` | ۸px | badge، دکمه کوچک، skeleton |
| `--radius-md` | ۱۲px | دکمه، input |
| `--radius-lg` | ۱۶px | کارت |
| `--radius-xl` | ۲۲px | کارت hero |
| `999px` | — | badge قرصی، آواتار |

**سایه:** سه سطح (`--shadow-sm/md/lg`). سایه‌ها بر پایه `#141A24` هستند نه مشکی خالص — تا با پس‌زمینه گرم هماهنگ بمانند.

---

## ۶. کامپوننت‌ها

همه در `src/components/ui/index.tsx`.

### دکمه — ۵ نوع، حداقل ۴۴px

| نوع | کاربرد |
|---|---|
| `primary` | کار اصلی هر صفحه — فقط **یکی** در هر نما |
| `secondary` | کار مهم ولی نه اصلی (حاشیه فیروزه‌ای) |
| `ghost` | کار کم‌اهمیت، لغو |
| `accent` | دستاورد/گیمیفیکیشن (زعفرانی) |
| `destructive` | حذف، خروج |

اندازه‌ها: `sm` (۳۶px) · `md` (**۴۴px** — پیش‌فرض) · `lg` (۵۲px).
دکمه قبلی ۳۸px بود — زیر حد لمسی. حالا پیش‌فرض ۴۴px است.

هر دکمه چهار حالت دارد: عادی، hover، active، disabled — به‌علاوه `loading` که اسپینر و متن «در حال انجام…» نشان می‌دهد و دکمه را قفل می‌کند.

### سایر کامپوننت‌ها

`Card` · `ButtonLink` · `SectionTitle` · `LevelBadge` · `SkillBadge` · `Progress` (با `role="progressbar"`) · `Stat` · `Empty` · **`ErrorState`** · `Spinner` · **`SkeletonText` / `SkeletonList` / `SkeletonCard` / `SkeletonChat`** · `Alert` · **`BridgeRing`**

---

## ۷. حالت‌ها — قانون سه‌گانه

هر بخشی که داده می‌گیرد باید هر سه را داشته باشد:

**لودینگ → skeleton هم‌شکل محتوای واقعی**
اسپینر وسط صفحه ممنوع (مگر اکشن کوتاه‌تر از ~۱ ثانیه). فهرست → `SkeletonList`. گفت‌وگو → `SkeletonChat` با حباب‌های چپ‌وراست.

**خالی → دعوت به عمل مشخص + دکمه**
❌ «داده‌ای موجود نیست»
✅ «هنوز درسی نساخته‌ای — درس‌ها بر اساس سطح و نقاط ضعف شما ساخته می‌شوند» + دکمه «ساخت اولین درس»

**خطا → چه شد + چه کنم + دکمه تلاش دوباره**
❌ «مشکلی پیش آمد»
✅ «تحلیل مربی هوشمند بارگذاری نشد — اتصال اینترنت را بررسی کن و دوباره تلاش کن.» + 🔄

قاعده مهم: **خطا هرگز به‌شکل محتوای عادی نمایش داده نمی‌شود.** مثلاً در چت مربی، خطای شبکه دیگر به‌صورت پیامی از زبان مربی («متأسفم، خطایی رخ داد») ظاهر نمی‌شود؛ به‌صورت نوار خطای سیستمی جدا با دکمه ارسال دوباره می‌آید.

---

## ۸. دسترس‌پذیری

| مورد | تصمیم |
|---|---|
| **فوکوس** | یک قاعده سراسری روی همه عناصر تعاملی: `outline: 2px solid` + `offset: 2px`. روی دکمه‌های رنگی، رینگ تیره + هاله سفید تا روی هر زمینه‌ای دیده شود. |
| **کنتراست** | حداقل ۴٫۵:۱ برای متن، ۳:۱ برای مرز عناصر تعاملی. |
| **رنگ تنها ممنوع** | درست/غلط = آیکون `✓`/`✗` + رنگ + متن مخصوص صفحه‌خوان. |
| **Skip link** | اولین توقف Tab در هر صفحه: «رفتن به محتوای اصلی». |
| **هدف لمسی** | حداقل ۴۴×۴۴px. |
| **ARIA** | `aria-current="page"` در ناوبری، `role="progressbar"`، `role="alert"` روی خطا، `aria-busy` روی skeleton، `aria-pressed` روی گزینه‌ها. |
| **ایموجی** | همه `aria-hidden="true"` — صفحه‌خوان اسم ایموجی را نخواند. |

---

## ۹. RTL

- **فقط ویژگی منطقی**: `ms-`/`me-`/`ps-`/`pe-`/`text-start`/`text-end`/`inset-inline-*`. هیچ `ml-`/`mr-`/`left-`/`right-` در کد باقی نمانده.
- **متن انگلیسی** همیشه در ظرف `dir="ltr"` با کلاس `.ltr`.
- **فلش‌ها**: در RTL «قبلی» به راست (`→`) و «بعدی» به چپ (`←`) اشاره می‌کند. در `OnboardingWizard` برعکس بود و اصلاح شد.

---

## ۱۰. حرکت

انیمیشن فقط در این نقاط:

| کجا | چه | چرا |
|---|---|---|
| حلقهٔ پل | پرشدن کمان (۰٫۹s) | پیشرفت را محسوس می‌کند |
| پاسخ درست | نبض ملایم مقیاس (۰٫۴s) | تأیید بدون سروصدا |
| پاسخ غلط | لرزش افقی ۴px (۰٫۳۲s) | «دوباره ببین»، نه تنبیه |
| نشان جدید | pop + درخشش | لحظه جشن |
| مربی در حال نوشتن | سه نقطه بالا-پایین | انتظار را قابل تحمل می‌کند |

**`prefers-reduced-motion: reduce` همه اینها را خاموش می‌کند** — شامل shimmer اسکلتون، fade-in، و transition دکمه‌ها.

---

## ۱۱. ناوبری

**موبایل:** نوار پایین ثابت با ۵ خانه — داشبورد · درس‌ها · لغات · مربی · بیشتر.
۹ آیتم قبلی در نوار بالای صفحه با اسکرول افقی بود؛ در گوشی با یک دست غیرقابل استفاده. حالا چهار کار روزانه در دسترس شست است و بقیه پشت شیت «بیشتر».

**دسکتاپ:** همان ۴ آیتم اصلی به‌صورت inline + منوی «بیشتر».

**منطق گروه‌بندی** حلقه یادگیری روزانه است: برنامه را ببین ← درس بخوان ← لغت مرور کن ← تمرین مکالمه. موارد دوره‌ای (تکالیف، تلفظ، گروه، پیشرفت، جدول، پنل مدرس) ثانویه‌اند.

---

## ۱۲. پیشنهادهای آینده

1. **دارک‌مود کامل** — الان فقط ۵ متغیر سطح عوض می‌شود. نسخه کامل نیاز به مقیاس رنگ جداگانه دارد.
2. **کلید دستی تم** — الان فقط تابع تنظیمات سیستم است.
3. **آیکون SVG به‌جای ایموجی** — ایموجی در پلتفرم‌های مختلف متفاوت رندر می‌شود.
4. **توکن‌های تایپوگرافی سیّال** با `clamp()`.

</div>
