<div dir="rtl">

# 🔍 ممیزی رابط کاربری و تجربه کاربری — زبان‌یار

> تاریخ: مرداد ۱۴۰۵ · مبنای تمام تصمیم‌های بازطراحی
> روش: خواندن کد + اجرای واقعی + اسکرین‌شات دسکتاپ/موبایل + محاسبه کنتراست

---

## ۱. فهرست صفحات و کامپوننت‌ها

**۲۲ صفحه · ۲۵ کامپوننت · ۵٬۱۹۶ خط**

| گروه | صفحات |
|---|---|
| عمومی | `/` · `/login` · `/signup` · `/offline` |
| یادگیری | `/dashboard` · `/lessons` · `/lessons/[id]` · `/placement` · `/vocabulary` · `/pronunciation` · `/assignments` |
| اجتماعی | `/tutor` · `/group-conversation` · `/leaderboard` |
| تحلیل | `/progress` · `/onboarding` |
| مدیریتی | `/admin` · `/teacher` + ۴ زیرصفحه |

بزرگ‌ترین کامپوننت‌ها: `PronunciationPractice` (۴۷۴)، `GroupChat` (۴۵۴)، `Leaderboard` (۲۹۶)، `TutorChat` (۲۷۹).

---

## ۲. وضعیت فعلی توکن‌ها

| مورد | وضعیت |
|---|---|
| فونت | وزیرمتن ✅ (سه وزن: ۴۰۰/۵۰۰/۷۰۰) |
| RTL پایه | `dir="rtl"` روی `<html>` ✅ |
| رنگ اصلی | آبی `#1d5cf5` — آبی پیش‌فرض و بی‌شخصیت |
| رنگ تأکیدی | سبز `#10b981` |
| دارک‌مود | ✅ تعریف شده ولی **فقط ۵ متغیر** — رنگ برند و سمانتیک تطبیق داده نشده |
| Tailwind | نسخه ۴ با `@theme` (بدون فایل config) |
| مقیاس spacing | ❌ تعریف‌نشده — مقادیر پراکنده |
| مقیاس تایپوگرافی | ❌ تعریف‌نشده — از `text-*` تیلویند مستقیم |

---

## ۳. مشکلات بصری (با شواهد)

### 🔴 بحرانی

**۳-۱. کنتراست زیر استاندارد WCAG AA**

محاسبه‌شده روی رنگ‌های فعلی:

| ترکیب | نسبت | وضعیت |
|---|---|---|
| `--muted` روی `--bg` | **۴٫۴۸:۱** | ❌ زیر ۴٫۵ — و این رنگِ بیشترِ متن‌های ثانویه است |
| `accent-500` روی سفید | **۲٫۵۴:۱** | ❌ رد شدید — رنگ حالت موفقیت |
| `--fg` روی `--bg` | ۱۶٫۷۹:۱ | ✅ |
| `brand-600` روی سفید | ۵٫۳۸:۱ | ✅ |

**۳-۲. هیچ `focus-visible` وجود ندارد**
`grep` روی `globals.css` → **صفر مورد**. کاربر کیبورد نمی‌فهمد کجاست. نقض مستقیم WCAG 2.4.7.

**۳-۳. بازخورد تمرین فقط با رنگ**
در `LessonExercises.tsx` پاسخ درست/غلط فقط با `borderColor` و `background` مشخص می‌شود — بدون آیکون یا متن. برای ۸٪ مردان با کوررنگی قابل تشخیص نیست. نقض WCAG 1.4.1.

### 🟠 مهم

**۳-۴. ۲۴ مورد margin/padding فیزیکی به‌جای منطقی**
`ml-1`, `mr-2`, `pl-`, `pr-` در ۹ فایل. در RTL معنی معکوس می‌دهند. مثال: `AppNav.tsx:64` آیکون منو با `ml-1` فاصله می‌گیرد که در راست‌چین سمت اشتباه است.

**۳-۵. فلش‌های جهت‌دار دستی**
`←` و `→` به‌صورت متن ثابت در ۸ فایل. در «بازگشت به درس‌ها ←» جهت با معنای ناوبری در RTL هم‌خوان نیست.

**۳-۶. رنگ برند بی‌شخصیت**
`#1d5cf5` آبی استاندارد بوت‌استرپ‌گونه است — هیچ ارتباطی با «یادگیری زبان» یا هویت ایرانی ندارد.

### 🟡 متوسط

**۳-۷. سلسله‌مراتب بصری داشبورد ضعیف**
در اسکرین‌شات: ۴ کارت آمار هم‌وزن، کارت مربی (آبی پررنگ) بصری‌ترین عنصر صفحه است در حالی که «کار امروز» باید باشد. کاربر نمی‌داند از کجا شروع کند.

**۳-۸. شعاع گردی ناهماهنگ**
`.card` = `1rem`، `.btn` = `.75rem`، badge = `999px`، برخی `rounded-xl`. بدون مقیاس.

---

## ۴. مشکلات تجربه کاربری

### 🔴 بحرانی

**۴-۱. ناوبری موبایل غیرقابل استفاده با یک دست**
در اسکرین‌شات موبایل (۳۹۰px): ۹ آیتم منو در یک نوار افقی اسکرول‌شونده **در بالای صفحه** فشرده شده‌اند. کاربر باید انگشت شست را تا بالای صفحه ببرد و افقی اسکرول کند تا آیتم پنهان را پیدا کند. **این مهم‌ترین مشکل UX پروژه است** چون اکثر زبان‌آموزان موبایل استفاده می‌کنند.

### 🟠 مهم

**۴-۲. پوشش حالت‌ها — جدول واقعی**

| کامپوننت | لودینگ | خالی | خطا |
|---|---|---|---|
| `VocabReview` | اسپینر | — | ❌ |
| `BadgeShelf` | اسپینر | — | ❌ |
| `TutorChat` | skeleton | — | ❌ |
| `LessonExercises` | — | — | ❌ |
| `PronunciationWorkshop` | — | — | ❌ |
| `CoachPanel` | skeleton | — | ❌ |
| `assignments/page` | — | ❌ | ❌ |
| `lessons/[id]` | — | — | ❌ |
| `Leaderboard` | skeleton ✅ | ✅ | ✅ |

**۹ کامپوننت هیچ حالت خطایی ندارند.** اگر شبکه قطع شود، کاربر صفحه سفید یا داده ناقص می‌بیند.

**۴-۳. لودینگ ژنریک**
۸ مورد `Spinner` وسط کارت به‌جای skeleton هم‌شکل محتوا. کاربر نمی‌داند چه چیزی در راه است.

**۴-۴. پیام‌های خطای مبهم**
`GenerateLessonButton` → «خطا در ساخت درس». نمی‌گوید چرا و چه کار کند.

### 🟡 متوسط

**۴-۵. اندازه هدف لمسی مرزی**
`.btn` با `padding: .625rem` ≈ ۳۸px ارتفاع. استاندارد اپل/گوگل ۴۴px است.

**۴-۶. ترتیب منو بدون منطق**
داشبورد ← درس‌ها ← مربی ← گفت‌وگوی گروهی ← مرور لغات ← تمرین تلفظ ← تکالیف ← پیشرفت ← جدول امتیاز. «تمرین روزانه» و «تحلیل» با هم قاطی‌اند.

---

## ۵. اولویت‌بندی

| # | مشکل | اثر | تلاش | اولویت |
|---|---|---|---|---|
| ۱ | ناوبری پایین در موبایل | 🔴 بسیار زیاد | متوسط | **P0** |
| ۲ | focus-visible | 🔴 دسترسی‌پذیری | کم | **P0** |
| ۳ | بازخورد فقط-رنگی | 🔴 دسترسی‌پذیری | کم | **P0** |
| ۴ | کنتراست AA | 🔴 دسترسی‌پذیری | کم | **P0** |
| ۵ | حالت‌های خطا/خالی | 🟠 زیاد | متوسط | **P1** |
| ۶ | هویت رنگی برند | 🟠 زیاد | کم | **P1** |
| ۷ | سلسله‌مراتب داشبورد | 🟠 زیاد | متوسط | **P1** |
| ۸ | margin منطقی (`ms-`/`me-`) | 🟡 متوسط | کم | **P1** |
| ۹ | skeleton به‌جای اسپینر | 🟡 متوسط | متوسط | **P2** |
| ۱۰ | مقیاس spacing/radius | 🟡 متوسط | کم | **P2** |

---

## ۶. پیشنهاد آینده (خارج از این مرحله)

- **دارک‌مود کامل**: الان فقط ۵ متغیر خنثی تغییر می‌کنند؛ رنگ برند و سمانتیک در تاریکی تطبیق داده نشده‌اند. نیاز به بازبینی جدا دارد.
- **کلید تغییر تم دستی**: الان فقط `prefers-color-scheme` است، کاربر نمی‌تواند دستی انتخاب کند.
- **آیکون‌های SVG**: الان همه‌جا ایموجی است. ایموجی روی پلتفرم‌های مختلف متفاوت رندر می‌شود و رنگش قابل کنترل نیست.

---

## ۷. آنچه تغییر نمی‌کند

طبق بریف: هیچ تغییری در API، RLS، ساختار جدول یا منطق کسب‌وکار. این بازطراحی صرفاً لایه ظاهری و تجربه است.

</div>
