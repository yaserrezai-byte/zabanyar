<div dir="rtl">

# 📦 اپلیکیشن اندروید زبان‌یار (APK)

## ✅ فایل آماده

| فایل | حجم | کاربرد |
|---|---|---|
| `dist-android/zabanyar-1.0.0.apk` | ۱.۲ مگابایت | **نصب مستقیم روی گوشی** |
| `dist-android/zabanyar-1.0.0.aab` | ۱.۳ مگابایت | ارسال به Google Play |
| `dist-android/android.keystore` | ۲.۷ کیلوبایت | 🔐 کلید امضا — نگهداری کنید! |

---

## 📲 نصب روی گوشی

۱. فایل `zabanyar-1.0.0.apk` را به گوشی منتقل کنید (تلگرام، USB، یا دانلود مستقیم).
۲. روی فایل بزنید.
۳. اگر پیام **«نصب از منابع ناشناس»** آمد، اجازه دهید:
   - **تنظیمات ← امنیت ← نصب برنامه‌های ناشناس** ← مرورگر/فایل‌منیجر را فعال کنید.
۴. روی **نصب** بزنید.

**بعد از نصب بررسی کنید:**
- ✅ آیکون آبی با حرف «ز» روی صفحه اصلی
- ✅ باز شدن بدون نوار آدرس مرورگر (تمام‌صفحه)
- ✅ نوار وضعیت آبی (`#1d5cf5`)
- ✅ فشار طولانی روی آیکون ← سه میان‌بر: مربی هوشمند، مرور لغات، تمرین تلفظ

---

## 🔍 این اپ چطور کار می‌کند؟

این یک **TWA** (Trusted Web Activity) است — روش رسمی گوگل برای تبدیل PWA به اپ اندروید:

</div>

```
┌──────────────────────────────┐
│  اپ نصب‌شده روی گوشی          │
│  ┌────────────────────────┐  │
│  │  موتور کروم (بدون UI)  │  │ ← تمام‌صفحه، بدون نوار آدرس
│  │  zabanyar-seven...     │  │
│  └────────────────────────┘  │
└──────────────────────────────┘
```

<div dir="rtl">

**یعنی:**
- ✅ حجم اپ فقط ۱.۲ مگابایت است (محتوا از سایت می‌آید)
- ✅ هر بروزرسانی سایت **فوراً** در اپ دیده می‌شود — نیازی به انتشار نسخه جدید APK نیست
- ✅ همان سرویس‌ورکر PWA کار می‌کند، پس حالت آفلاین هم فعال است
- ⚠️ برای بار اول به اینترنت نیاز دارد

### تأیید Digital Asset Links

برای اینکه اپ **بدون نوار آدرس** باز شود، فایل زیر روی سایت منتشر شده و اثر انگشت گواهی امضا را به اپ متصل می‌کند:

`https://zabanyar-seven.vercel.app/.well-known/assetlinks.json`

این اتصال با API رسمی گوگل تأیید شده است ✅

</div>

```
SHA-256: A3:68:C6:4C:07:33:95:86:0E:29:E8:83:B4:AD:A7:80:4C:D5:DB:9B:8F:AE:DF:CA:E2:79:16:EA:11:FA:BD:0C
```

<div dir="rtl">

> ⚠️ اگر روزی کلید امضا را عوض کردید، **باید** این اثر انگشت را هم در `public/.well-known/assetlinks.json` به‌روز کنید، وگرنه اپ با نوار آدرس باز می‌شود.

---

## 🔐 نگهداری از کلید امضا (بسیار مهم)

فایل `android.keystore` و رمز آن را **حتماً جای امنی نگه دارید**.

| اگر گم شود | نتیجه |
|---|---|
| بروزرسانی اپ | ❌ غیرممکن — کاربران باید اپ را حذف و دوباره نصب کنند |
| انتشار در Play | ❌ باید با نام بسته جدید شروع کنید |

رمز کلید در فایل `~/.secrets/keystore-pass.txt` روی محیط ساخت ذخیره شده است. یک نسخه پشتیبان از هر دو تهیه کنید.

> 🔒 این فایل‌ها در `.gitignore` قرار گرفته‌اند و هرگز به مخزن Git نمی‌روند.

---

## 📋 مشخصات فنی

| مورد | مقدار |
|---|---|
| Package ID | `app.zabanyar.twa` |
| نسخه | ۱.۰.۰ (versionCode: 1) |
| حداقل اندروید | ۵.۰ (API 21) |
| هدف | API 36 |
| امضا | v1 + v2 + v3 ✅ |
| مجوزها | فقط اعلان‌ها (`POST_NOTIFICATIONS`) |

---

## 🔄 ساخت مجدد (نسخه جدید)

اگر خواستید نسخه جدید بسازید:

</div>

```bash
export JAVA_HOME=/home/user/android-build/jdk17
export ANDROID_HOME=/home/user/android-build/sdk
export PATH=$JAVA_HOME/bin:$PATH
cd /home/user/android-build/twa

# شماره نسخه را بالا ببرید
#   app/build.gradle → versionCode 2, versionName "1.0.1"

./gradlew assembleRelease --no-daemon

# زیراِلاین و امضا
BT=$ANDROID_HOME/build-tools/34.0.0
$BT/zipalign -p -f 4 \
  app/build/outputs/apk/release/app-release-unsigned.apk /tmp/aligned.apk
$BT/apksigner sign --ks android.keystore --ks-key-alias zabanyar \
  --out zabanyar-new.apk /tmp/aligned.apk

# بررسی امضا
$BT/apksigner verify --verbose zabanyar-new.apk
```

<div dir="rtl">

> 💡 **یادآوری:** چون محتوا از سایت می‌آید، برای تغییر متن‌ها، درس‌ها یا قابلیت‌ها **نیازی به ساخت APK جدید نیست**. فقط وقتی APK جدید لازم است که آیکون، نام اپ یا تنظیمات خودِ اپ عوض شود.

---

## 🍎 و آیفون؟

اندروید APK دارد، اما اپل اجازه نصب فایل مستقیم نمی‌دهد. برای آیفون از همان روش PWA استفاده کنید:

**Safari ← دکمه اشتراک‌گذاری ← Add to Home Screen**

نتیجه عملاً یکسان است: آیکون روی صفحه اصلی و اجرای تمام‌صفحه.

</div>
